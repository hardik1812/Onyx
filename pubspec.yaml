name: fastreminder
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  flutter_rust_bridge: 2.12.0
  image_picker: ^1.2.2
  path: ^1.9.1
  path_provider: ^2.1.5
  rust_lib_fastreminder:
    path: rust_builder
  video_player: ^2.11.1
  timer_builder: ^2.0.0
  android_intent_plus: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
